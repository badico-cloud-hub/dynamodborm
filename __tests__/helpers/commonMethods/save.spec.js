import { save } from '../../../src/helpers/commonMethods/save'

describe('save commom method', () => {
    it('should use the connection update method', () => {

    })

    it('should handle createdAt and updatedAt fields in a object without id', () => {

    })

    it('should handle updatedAt field in a object with id', () => {

    })

    it('should return the same instance of the object', () => {

    })
})